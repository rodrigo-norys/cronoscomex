# 10 — Governança

Documento de **duas naturezas**, e a distinção evita tanto a cerimônia quanto a
poda errada. §1 a §4, §6 e §7 são o **processo**: curtas e acionáveis, e é delas
que vale a promessa de brevidade. §5 é **registro append-only** — cresce a cada
decisão, não se lê inteira, e se consulta pela decisão que interessa. Um
desenvolvedor, um usuário final, sem comitê — processo pesado aqui morreria na
segunda história.

Ele existe por um motivo concreto: **o escopo deste projeto oscilou antes da
primeira linha de código** — `D-05`, `D-06` e `D-07` são o mesmo tema decidido
em três rodadas, todas em 03/08/2026 —, e cada mudança custou retrabalho de
documentação que ninguém registrou. As seções abaixo cobrem exatamente isso.

---

## 1. Quem decide o quê

| Decisão | Quem decide | Registro |
|---|---|---|
| Escopo funcional — o que o produto faz | **Usuário** | §5 deste documento |
| Prioridade e ordem de entrega | **Usuário** | `07-plano-entrega.md` |
| Prazo, orçamento, quem mantém depois | **Usuário** | — |
| Arquitetura, stack, modelo de dados | **Implementação** | ADR em `docs/adr/` |
| Como uma regra da especificação é resolvida | **Implementação** | Achado A-NN em `01-auditoria-especificacao.md` |
| O que fazer quando o dado real contradiz a spec | **Regra fixa:** o dado vence | `00-visao-escopo.md §6.1` |
| Valor de limiar sem base declarada (ex.: 15 dias de "parado") | **Implementação**, como premissa marcada | `00-visao-escopo.md §4` |
| Forma da interface — layout, densidade, onde uma peça vive | **Usuário**, escolhendo entre alternativas que a implementação desenha e **mede** | §5 deste documento |

**A fronteira em uma frase:** o usuário decide *o que* o sistema faz; a
implementação decide *como* e assume o ônus de registrar por quê.

**A linha de forma tem procedimento, e ele estava sendo praticado sem estar
escrito** — `D-21`, `D-22`, `D-30`, `D-31`, `D-36` e `D-37` seguiram todas o
mesmo caminho: a implementação desenha ao menos duas formas com os tokens do
projeto, **mede** o que é mensurável (contraste, largura a 320 px, altura
renderizada), recomenda uma, e **registra a recusa** quando o usuário escolhe
outra. A recomendação recusada é ativo, não ressalva a repetir — `D-30` escolheu
`3a` contra a recomendação de `3b`, e é o registro dela que impede a discussão
voltar.

---

## 2. Protocolo de mudança de escopo

Disparado quando o usuário pede algo que contradiz o plano vigente.

1. **Não implementar em silêncio.** Uma mudança de escopo acomodada sem
   registro produz documentação que mente sobre o sistema.
2. **Declarar o custo antes de aceitar**, em duas ou três frases: o que sai do
   escopo, o que entra, quais histórias mudam, qual ADR é afetado.
3. **Se a mudança contradiz uma resposta anterior do próprio usuário, apontar
   a contradição e pedir desempate.** Aconteceu com "planilha somente-leitura"
   × "dashboard somente-leitura", que juntas congelariam a base.
4. **Aplicar em cascata, e o elo entra quando a decisão o alcança:** ADR
   afetado → `00-visao-escopo.md` (se muda o que o produto faz) →
   `02-requisitos.md` (se contraria ou cria RF/RNF) →
   `01-auditoria-especificacao.md` (se contraria um `A-NN` — regra inviolável 1)
   → `05-contratos-api.md` (se toca contrato de rota) → `06-backlog.md` →
   `09-rastreabilidade.md` → `CLAUDE.md`, bloco Estado (só o que fica aberto).
   `07-plano-entrega.md` recebe apenas decisão do **plano original**; épico
   posterior registra a ordem no próprio cabeçalho. **O elo que a decisão não
   alcança se declara, não se omite** — foi a omissão silenciosa que `D-26`
   pagou para descobrir. Esta lista dizia cinco documentos e omitia
   `02-requisitos.md` e `05-contratos-api.md`, que são o terceiro e o quinto
   arquivo mais tocados pelos commits deste documento.
5. **Registrar no log da §5**, com data.

**Regra de reafirmação:** se uma preocupação é levantada e o usuário mantém a
decisão, a decisão é dele. Registre a ressalva uma vez, no documento
apropriado, e siga — sem repetir a objeção a cada oportunidade.

---

## 3. Ciclo de vida de um ADR

| Estado | Significado |
|---|---|
| **Aceito** | Em vigor. É a decisão a seguir |
| **Substituído por ADR-NNNN** | Continua no repositório, com o link para o sucessor. **Nunca apagar** — o raciocínio descartado é o que impede redecidir errado |
| **Revogado** | A decisão deixou de valer e nada a substitui |

Um ADR é reaberto quando **seu gatilho declarado dispara**. Os gatilhos vivos:

| ADR | Gatilho de reabertura | Fonte |
|---|---|---|
| 0006 · indicadores em memória | Parse > 10 s (RNF-13) ou processo > 512 MB (RNF-16) | R-10 |
| 0002 · aplicação local sem banco | Um **segundo usuário** simultâneo passar a existir | ADR-0002, "Negativas" |
| 0002 · candidato Power BI eliminado | Colunas `RESPONSÁVEL` e `CANAL` em texto passarem a existir | ADR-0002, K4 |
| 0004 · escrita cirúrgica | Nenhum — é decisão de segurança, não de conveniência | — |
| 0003 · chave de estilo literal | Alteração da **definição** de um estilo no Excel, causando pico de `COR_NAO_MAPEADA` | ADR-0003 |
| 0005 · histórico JSONL | Nenhum previsto | — |
| 0001 · planilha como fonte da verdade | O mesmo de 0006 — o volume deixar de caber em memória. O princípio em si não reabre: revê-lo é redefinir o produto | ADR-0001, "Cada leitura reprocessa tudo" (R-10) |

**Alterar um ADR aceito não se faz reescrevendo o texto antigo.** Duas formas
valem, e as duas preservam o que foi superado:

1. **ADR novo que substitui** o anterior, que passa a *Substituído por ADR-NNNN*.
2. **Emenda datada dentro do próprio ADR**, em bloco citado, dizendo o que
   deixou de valer, o que passa a valer e o `D-NN` que autorizou.

**A segunda é a que o projeto de fato usa, e esta seção não a registrava.** São
quatro emendas datadas em vigor — duas no `ADR-0004` (06/08/2026, sobre
`xl/styles.xml`; 02/09/2026, por `D-25`), uma no `ADR-0003` (02/09/2026, por
`D-25`) e uma no `ADR-0005` (17/08/2026, por `H-28`) —, mais a seção de
validação empírica que `H-01` acrescentou ao `ADR-0003`. **Nenhum ADR novo
nasceu desde o 0006.** O que continua proibido é a terceira forma: trocar o
texto antigo sem deixar rastro.

---

## 4. Definition of done

### História (H-NN)

- [ ] O protocolo de fatia (`CLAUDE.md`) foi apresentado e aprovado
- [ ] Todos os critérios de aceite passam
- [ ] Todos os casos-limite da história têm teste com **valor concreto**
- [ ] `npm run verify` passa — os sete passos: `test:hooks`, `test:dados`, `test:strip`, lint, typecheck, testes, build
- [ ] Nenhuma regra de negócio fora de `src/domain/`
- [ ] Nenhum teste aponta para a planilha real
- [ ] Conferida contra a **planilha real** por `tools/carregar-planilha.mjs`, e o
      número medido foi para o bloco `✅ CONCLUÍDA`
- [ ] `revisor-xml` invocado e verde, **se** a fatia tocou `xlsx-surgeon.ts`,
      `write-guard.ts` ou qualquer código que reescreva bytes do `.xlsx`
- [ ] PR aberto, com `verify` e `dados-sensiveis` verdes — o merge é do dono, e a
      `main` protegida não aceita outro caminho
- [ ] História marcada em `06-backlog.md`
- [ ] `09-rastreabilidade.md` conferido, se algum status mudou
- [ ] `CLAUDE.md`, bloco Estado: pendência criada acrescentada, pendência fechada
      removida — e nada mais
- [ ] Comentário novo diz o que o código não diz, e todo fato medido nele cita a
      fonte (`A-NN`, `TD-NN`, `H-NN`) — régua em `.claude/rules/comentarios.md`

**Os passos operacionais estão em `/fechar-historia`; aqui fica só o que precisa
ser verdade.**

### Fase

- [ ] Todas as histórias da fase concluídas
- [ ] O **critério de saída** declarado em `07-plano-entrega.md` foi verificado
      com número, não com impressão
- [ ] Riscos da fase reavaliados: algum encerrou, algum surgiu?
- [ ] O usuário conseguiu usar o entregável para uma tarefa real

### Projeto

- [ ] Todos os itens de `09-rastreabilidade.md` implementados ou explicitamente
      bloqueados com motivo
- [ ] `README.md` da raiz permite instalar e operar sem consultar mais nada
- [ ] Restauração de backup testada na máquina alvo, não só no teste automático
- [ ] `formatado.xlsx` aberto no **Excel real** sem aviso de reparo

---

## 5. Log de decisões do usuário

Registro do que foi decidido, para não ser re-litigado. Data no formato
AAAA-MM-DD.

| # | Data | Decisão | Custo / consequência |
|---|---|---|---|
| D-01 | 2026-08-03 | O arquivo é `.xlsx` local sincronizado por OneDrive, não Google Sheets | Fixou a estratégia de leitura |
| D-02 | 2026-08-03 | **Amarelo forte = importador fora do RJ**, não Canal Amarelo | Canal Amarelo fica sem representação estruturada (A-38) |
| D-03 | 2026-08-03 | **Um único usuário** | Dispensou autenticação e RLS |
| D-04 | 2026-08-03 | **Sem colunas novas** | IND-21 permanece bloqueado; custo de cada coluna adiada em `03-modelo-dados.md §5` |
| D-05 | 2026-08-03 | **Sem Supabase, sem banco, sem nuvem** — aplicação 100% local | Reverteu a virada de escopo do briefing. Eliminou 3 ADRs (persistência, autenticação, hospedagem) e o épico de sincronização. Custo: sem acesso remoto |
| D-06 | 2026-08-03 | O painel **também edita**, gravando de volta no `.xlsx` | Acrescentou o épico E5 (5 histórias) |
| D-07 | 2026-08-03 | **Não escrever direto na planilha de produção** — fila local, aplicação sob comando explícito | Acrescentou `pending-edits.jsonl` e as 6 defesas de `H-25`. Reverteu parcialmente D-06 |
| D-08 | 2026-08-03 | Histórico em **arquivo local JSONL** | Destravou ALE-06 e a Página Histórico (ADR-0005) |
| D-09 | 2026-08-03 | Entrega como **app no navegador servido por Node local** | Descartou Electron e SPA hospedada |
| D-10 | 2026-08-03 | **Apenas a aba `2026`** | Abas `2025` e `2024` fora; `CNPJ` excluída por conter credenciais |
| D-11 | 2026-08-03 | Os dois tons de verde e os dois de roxo são **o mesmo significado** | 4 entradas no `color-map.json` para 2 significados |
| D-12 | 2026-08-03 | **A planilha é a referência prioritária**, acima da especificação | Virou princípio: `00-visao-escopo.md §6.1` |
| D-13 | 2026-08-03 | **Biome 2.5.6** como lint e formatação | O plano exigia `npm run lint` sem escolher a ferramenta — lacuna encontrada pelo protocolo de fatia em `H-02`. Uma dependência em vez de três; regra de fronteira via `overrides` + `noRestrictedImports`, confirmada na documentação oficial |
| D-15 | 2026-08-03 | Regra 10 reformulada: de "nunca leia as abas fora de escopo" para **"nunca processe, indexe, exponha nem registre"** | A redação original era inatingível — o pool `sharedStrings` é global ao arquivo. A nova é verificável por teste. Lacuna encontrada pelo protocolo de fatia em `H-03` |
| D-14 | 2026-08-03 | **Node 22.x LTS** nas duas máquinas | Alinha desenvolvimento e produção. Fixado em `.nvmrc` e em `engines` do `package.json` |
| D-17 | 2026-08-06 | **Testing Library 16.3.2 + jsdom 30.0.1** como quarta camada de teste, preparada **antes** da primeira história de interface | O plano não previa teste de frontend em lugar nenhum: `08-qualidade-operacao.md §1` define três camadas — domínio, I/O e ponta a ponta — e as histórias `H-16` a `H-22` listam só arquivos `.tsx`. Lacuna encontrada pelo protocolo de fatia em `H-15`. **Não contradiz D-16:** são `devDependencies`, não entram no bundle nem chegam à máquina do operador — o `react-router` era runtime. `jsdom` e não `happy-dom` porque o mais rápido é incompleto justamente em `history.pushState`, `popstate` e `visibilitychange`, que são o que a casca precisa testar. Ficaram de fora `user-event` e `jest-dom`: o `fireEvent` e o `expect` do Vitest cobrem, e acrescentam-se quando doer. Custo: duas dependências de desenvolvimento e dois projetos no `vitest.config.ts` |
| D-16 | 2026-08-06 | **Roteamento à mão**, com `History API` e `URLSearchParams`. **`react-router` fica registrado como reavaliação futura**, não como recusa definitiva | São **sete páginas planas**, sem rotas aninhadas nem carregamento por rota, numa aplicação local — e o plano proíbe dependência não prevista. O benefício mais concreto do `react-router` seria `useSearchParams`, que é camada fina sobre o `URLSearchParams` nativo. Custo do caminho escolhido: ~40–60 linhas próprias, e a obrigação de tratar `popstate` para o botão "voltar" do navegador funcionar — que vira teste, não risco. **Reavaliar se:** as páginas passarem de ~10, aparecerem rotas aninhadas ou parâmetros de rota além de `/processo/:ref`, surgir necessidade de carregamento por rota, ou o roteamento próprio ultrapassar ~100 **linhas de código**. Qualquer um desses gatilhos torna a dependência mais barata que a manutenção. O gatilho de tamanho contava o arquivo inteiro até 17/08/2026, e media a coisa errada: comentário de porquê é 24% de `router.ts` por política, então um arquivo bem documentado disparava antes de um mal documentado com o dobro de fluxo. **Gatilhos observados em 17/08/2026, um atingido e a decisão mantida:** `web/src/router.ts` tem **132 linhas** no total, mas **79 de código** — sob o limiar, e o `react-router` não removeria as outras 53, que são comentário e espaço; e **passou a haver carregamento por rota**, com a Página Histórico sob demanda, resolvido pelo `lazy` do próprio React em 3 linhas de `App.tsx`, sem dependência nova. Os outros três seguem abertos, e **um deles encostou no limiar em 31/08/2026**: `H-70` levou `router.ts` de 79 a **97 linhas de código** de ~100, ao pôr ali o sinal de foco pendente que a casca consome — três linhas de folga, e a próxima fatia que tocar o arquivo provavelmente dispara a reavaliação. Continuam sete páginas de ~10, e nenhuma rota aninhada. `tests/repo/contratos.test.ts` mede os quatro a cada execução e **reprova** se um for atingido sem estar registrado aqui — o de 132 linhas passou despercebido por dias, e foi o que motivou a asserção. **`H-59` redesenhou a navegação em 01/09/2026 e o arquivo continuou em 97 linhas de código**, porque a separação entre destino de dado e rodapé ficou em `web/src/components/AppSidebar.tsx`: ela é apresentação, e pô-la no roteador teria disparado o gatilho por um dado que não é de roteamento. As sete páginas e a ausência de rota aninhada seguem iguais |
| D-18 | 2026-08-07 | `web/` importa os tipos de resposta **das próprias rotas**, com `import type` — nunca valor, nunca função | O esqueleto de `H-02` redeclarava `HealthResponse` no cliente com **metade** dos campos, e nada obrigava as duas declarações a concordar: é assim que um contrato diverge em silêncio. Com `verbatimModuleSyntax`, o import é apagado na compilação — **medido** ao fechar `H-15 ②`: `grep 'fastify\|process-store\|exceljs'` no bundle de produção devolve **0**, e o `vite build` transforma 22 módulos. **O limite é estrito e a razão dele é a regra inviolável 6:** só `import type`, e só de `src/http/routes/`. Importar valor traria `fastify` e o `process-store` para o navegador; importar de `src/domain/` traria regra de negócio para o cliente, que é exatamente o que a regra proíbe. **Não afrouxa a regra 5**, que é sobre `src/domain/` não importar para fora — a direção aqui é a oposta, e o `noRestrictedImports` do Biome continua guardando aquela |
| D-19 | 2026-08-04 | **Permissões do agente em `.claude/settings.json`:** `git add`, `git push`, `npm install` e `npm ci` pedem confirmação; `curl`, `wget`, force-push e leitura ou escrita de `*.xlsx` e `*.jpeg` da raiz estão negados; o modo bypass fica desabilitado | Custo: uma confirmação por operação de staging e de rede. Ganho: nenhuma via de saída de dado passa sem portão. *(**`git add` e `git push` deixaram de pedir confirmação em 31/08/2026** e estão em `allow` desde então, ao abrir a sessão sem supervisão: o que passou a proteger é o portão antes do commit mais a `main` protegida. No mesmo commit entraram as negações que faltavam — `reset --hard`, `clean`, `checkout --`, `rebase`, `commit --amend` e `branch -D` —, que são o outro lado da troca. `npm install`, `npm ci` e as negações de rede seguem como esta linha decidiu.)* |
| D-20 | 2026-08-04 | **`mcp__*` negado — todo MCP, de todo servidor** | Conector é caminho de saída que **não passa** pelas regras de `Bash` nem de arquivo: negar `curl` e deixar `sharepoint_upload_file` aberto seria o inverso do modelo de ameaça. O glob no nome da ferramenta cobre conector que ainda nem foi criado, e o cliente remove as ferramentas negadas do contexto. `deny` vence `allow`, então **não há exceção parcial**: para usar um conector, remova a linha deliberadamente |
| D-21 | 2026-08-31 | **O modo escuro entra no plano.** A interface passa a ter dois esquemas de cor, escolhidos por `prefers-color-scheme`, **sem alternância manual** | Reverte a determinação do cabeçalho de `E9`, que dizia "funcionalidade nova, fora deste épico e fora do plano" — a frase segue verdadeira sobre `E9` e deixa de valer como recusa. Consequências: `D03`–`D07` do corpus de estilo saem da condicionalidade e passam a incidir; a superfície de contraste a verificar dobra, e por isso `H-65` reexecuta os seis procedimentos de navegador nos dois esquemas; **toda história posterior a `H-57` que acrescentar token de cor declara os dois esquemas**, sem o que `H-51` e as fatias seguintes nasceriam com metade da paleta. `D04` **não** incide: sem alternância manual, a variante `dark:` da v4 resolve para a media query sem `@custom-variant`. As outras 23 dispensas de `E9` continuam de pé |
| D-22 | 2026-08-31 | **A casca é redesenhada** conforme `docs/redesign/PROPOSTA.md` — navegação lateral, topo de uma linha, filtros como chips, dois raios, densidade de 40 px, movimento com redução, e o acento deixando de ser neutro | Acrescenta o épico `E11`, nove histórias sobre 25 arquivos de `web/src/`. **Nenhum indicador muda de valor e nenhuma rota muda de contrato.** `E9` e `E10` passam a precedê-lo: `H-45` e `H-46` tocam os mesmos arquivos, e `H-47` é a linha de base da verificação. **A medição já reprovou seis pares da paleta proposta**, três deles reintroduzindo defeitos que `H-39` e `H-40` haviam removido — `H-57` nasce com as correções calculadas em `PROPOSTA.md §2.2`. Duas peças do mockup ficam declaradas em aberto e **não** viram história: a busca `⌘K` e o detalhe em painel lado a lado. *(**Uma das duas deixou de estar em aberto:** a busca virou `H-83` por `D-29`, em 03/09/2026, e `D-34` estendeu o alcance dela a seis campos. O detalhe em painel lado a lado continua fora.)* |
| D-23 | 2026-08-31 | **Sem `config/team-map.json`, `responsible` recebe a chave de cor** — o campo segue mostrando o que a cor diz, e a resolução declara `source: 'cor'` | Fecha a Pendência 1 de `docs/sessao-autonoma/RELATORIO-31-08-2026.md`, que travava `H-50`. O quinto critério de aceite afirmava um mecanismo que não existe: com o mapa vazio, o desempate por cor procura um **membro** cuja lista de cores case, e sem membro nenhum `resolveTeam` devolve `UNASSIGNED` nas 649 — nem "cai no desempate", nem "o comportamento é o de hoje", que tem 165 preenchidos. **O estado sem mapa é o inicial garantido:** o arquivo está no `.gitignore` e `scripts/sincronizar-distribuicao.ts` leva só o `.exemplo`, então o operador chega sem ele — é o cenário de `PD-01`. Alternativa recusada: o campo ficar vazio até o mapa existir, com a cor visível só em `colorResponsible`; ela é mais limpa no domínio e entrega ao operador, na primeira execução, três telas piores que as de hoje. Custo do caminho escolhido: o domínio de `Responsible` vira união — chave de membro **ou** chave de cor —, habitada **só** enquanto o mapa estiver vazio; e nesse estado o filtro Responsável perde a agregação de A-18, que migra para `colorResponsible`. Precedente no próprio repositório: `resolveClient` cai para a grafia da célula e expõe `mapped: false`. **Executada em `H-50`, 01/09/2026, com uma correção de número:** esta linha dizia "157 preenchidos", copiado de `docs/uso/RESULTADO.md §3`, e o valor medido é **165** — o erro era do documento de origem, não da decisão, que não depende do número *(**REABERTA por `D-40` em 10/09/2026:** com a tela de `H-91`, o argumento que a sustentava — o campo Responsável nascer vazio nas 649 linhas — deixa de valer, e `H-93` tira a cor dos níveis 0 e 3 de `resolveTeam`. O que era desempate vira "Sem responsável": 48 processos migram, todos ativos.)* |
| D-24 | 2026-08-31 | **`H-50` é cortada, e a metade que sobra é declarada G** — a primeira do backlog. O resto de tela vira `H-66` | Fecha a Pendência 2 de `docs/sessao-autonoma/RELATORIO-31-08-2026.md`. `H-50` declarava `M (15 arquivos, contrato de três rotas alterado)` desde que nasceu, e a régua do topo de `06-backlog.md` diz `M = até 8 arquivos ou 1 contrato novo` — era G rotulada como M. **O corte a leva a 12 arquivos, e 12 ainda é G:** as divisões que a fariam caber em `M` foram tentadas e custam mais do que economizam — separar o campo do indicador é impossível, porque IND-20 e IND-22 leem `process.responsible` e mudam junto; e fatiar o domínio antes das rotas deixaria um intervalo com o Responsável fora da tela, entre dois PRs, ou com o campo duplicado no contrato. **Nenhuma G existia em 65 histórias** — o rótulo sempre foi o sinal de cortar antes de executar —, e a decisão é que aqui ele passa a ser o que a régua também serve para fazer: avisar que a fatia é longa. `H-66` fecha o quinto critério de `H-53`, que estava não-incidente. **A numeração é `H-66`, e não `H-50b`, por impedimento medido:** `tests/repo/contratos.test.ts` casa `H-\d+` e `h-\d+` estritos em três asserções — o sufixo de letra faria duas seções colidirem no mesmo id e deixaria âncora e índice fora do conjunto comparado. Manter `H-50` viva também preserva as quatro âncoras dela em `src/`, `tests/` e no `.exemplo` |
| D-25 | 2026-09-02 | **A criação de linha nova entra no escopo.** A Página Operacional passa a inserir processo, e a cirurgia ganha `appendRow` | Reverte `docs/00-visao-escopo.md §3.2`, a negativa do ADR-0004 e a frase de `05-contratos-api.md §3` — os três foram emendados no mesmo dia. **A medição no arquivo real é o que tornou a extensão barata:** a aba `2026` não tem formatação condicional, validação nem autofiltro próprios, e `Tabela1` cobre `A1:P997` contra 745 linhas escritas — enquanto houver folga, a linha nova cabe sem estender intervalo nenhum, e quando não houver a cirurgia **recusa**. Consequências: a linha nasce com o estilo da COLUNA (`xf 162`, sem preenchimento), porque o da linha de cima carrega `fillId 8` = Colaborador 1; e `resolveColor` passou a distinguir **ausência de cor** de **cor desconhecida**, senão toda linha criada iria para a quarentena. O `revisor-xml` reprovou **duas vezes**: seis achados em `appendRow` e quatro em código já commitado na primeira, e mais cinco na segunda — dois deles **introduzidos pelas correções da primeira**, que é o motivo de a régua mandar reinvocá-lo depois de corrigir. Um dos quatro descartava `<si/>` do pool GLOBAL de strings — defeito presente desde `H-24`, alcançável por `applyCellEdits`. A remoção de linha **continua fora de escopo**, e desde 03/09/2026 está escrita como tal em `docs/00-visao-escopo.md §3.2`. **Emenda de 03/09/2026:** a negativa que esta decisão diz ter revertido **nunca esteve em §3.2** — o que havia era §3.1 sem a criação de linha na linha de Edição. Os dois lados foram corrigidos com `E13`, em `D-26` |
| D-26 | 2026-09-03 | **O trabalho de 02/09/2026 ganha épico retroativo — `E13`, com `H-77` a `H-81`** —, e a cascata de documentos que ele não percorreu é percorrida agora | O PR #111 entrou com cinco commits de produto e 49 arquivos **sem história no backlog**, e o commit de documentação do mesmo dia (`603a62f`) alcançou `05-contratos-api.md`, este documento e o `ADR-0004` — deixando de fora `00-visao-escopo.md`, `02-requisitos.md`, `06-backlog.md` e `09-rastreabilidade.md`. Achado pela auditoria de alinhamento de 03/09/2026. **Alternativa recusada:** deixar como está e registrar a lacuna, que foi o que aconteceu com `PD-05` entre 14 e 17/08/2026 e com `P-15` (`PD-09`) desde `H-30` — nas duas vezes o item ficou sem dono. **O que a escrita retroativa recupera é a rastreabilidade, e o que ela não recupera é o protocolo de fatia**, cujo valor é o defeito de plano aparecer antes do código; por isso cada bloco de `E13` cita o commit que a entregou, em vez de fingir que planejou. Consequências medidas: o backlog vai de 76 para **81** histórias, com duas G novas — `H-79` (29 arquivos, 2 rotas) e `H-80` (15 arquivos) —, e nasceram **cinco RF**: RF-32 a RF-35 de `E13`, e **RF-31 para a Página Configuração**, entregue entre `H-34` e `H-38` e sem requisito desde agosto. `docs/00-visao-escopo.md §3.1` passa a listar a criação de linha e a Página Configuração, e §3.2 passa a declarar a **remoção** de linha como fora de escopo, que `D-25` decidiu e nenhum documento registrava |
| D-27 | 2026-09-03 | **A cobertura de teste continua desligada, e os documentos param de afirmar que ela é portão** | `vitest.config.ts` declara `coverage.thresholds` nos quatro eixos em `0` e `npm test` é `vitest run` **sem** `--coverage` — então nem o percentual é medido —, enquanto `02-requisitos.md` afirmava RNF-35 ≥ 90% e RNF-36 ≥ 80% sem ressalva e `08-qualidade-operacao.md §5.2` dizia que `npm test` roda "com os limiares de cobertura de RNF-35 e RNF-36". **Alternativa recusada: ligar os limiares.** O que a lacuna real pede é `PR-10` — os 43 casos-limite obrigatórios de `08-qualidade-operacao.md §1.3` virando teste —, e percentual de linha não verifica isso; ligar limiar por camada é mudança de código com o backlog fechado, e o número que ele reprovaria não é o número que interessa. Os dois RNF ficam como **alvo declarado**, com a ressalva escrita em §1.1, no comentário do próprio `vitest.config.ts` e na linha de cada requisito. Quem quiser o número roda `npx vitest run --coverage`. *(**Esta linha diz 43 casos-limite, e são 44** — contados na tabela de `08-qualidade-operacao.md §1.3`, que afirma o mesmo número, como o `CLAUDE.md`. A varredura que consertou este número nos demais arquivos não passou por aqui. A decisão não depende dele: o que a lacuna pede continua sendo `PR-10`.)* |
| D-28 | 2026-09-03 | **As fontes passam a chegar na máquina do operador; a linha de teste na planilha de desenvolvimento fica** | Dois achados da mesma auditoria, com desfechos opostos de propósito. **As fontes:** os seis `.woff2` de `H-58` e a licença OFL nunca entraram na branch `distribuicao`, porque o fecho transitivo de `scripts/sincronizar-distribuicao.ts` tratava `.css` como folha e não alcançava o `url("/fonts/...")` — e como fonte ausente não produz erro, o navegador caía no fallback e o script imprimia "sincronizada com HEAD". Corrigido na **causa**, e não com lista manual: o fecho passa a ler os `url()` absolutos do CSS, e `tests/repo/distribuicao.test.ts` reprova se um asset citado ficar fora. **A linha de teste:** `CONTROLE DOS EMBARQUE.xlsx` na raiz do projeto ficou com 746 linhas — `A746 = FT5555.28` —, escrita em 02/09/2026 pelo caminho novo de criação de linha. É a **cópia local** de desenvolvimento, e a do OneDrive não foi tocada; apagá-la pela aplicação exigiria a remoção de linha, que `D-25` manteve fora de escopo, e editar o arquivo à mão contraria a regra de que ele é a referência e não um artefato de teste. **Fica como está, com o registro aqui** — a planilha de desenvolvimento não é fonte de nenhum número afirmado nos documentos, que vêm de `H-01` e das fixtures |
| D-29 | 2026-09-03 | **As quatro melhorias de interação da lateral entram, e viram `E14`** — ícone por destino, contagem que segue o recorte, carregamento sem salto e busca por atalho | Pedido do usuário a partir do mockup do redesenho, com as quatro opções desenhadas e comparadas antes do aceite. **Três delas já estavam especificadas em `docs/redesign/PROPOSTA.md` e nunca viraram história em `E11`** — o ícone inclusive tem par de cor medido (`chart-1`, 6,01:1), num trecho de §2 que registra um alarme falso de contraste e descreve um item ativo que a implementação não desenha. Três escolhas do usuário fecham o que seria verbo de decisão em aberto: **(1) o ícone herda a cor do texto**, sem cor nova no conjunto, e `chart-1` fica como alternativa recusada; **(2) a contagem conta o que a página mostra**, e não o total do arquivo, para o número da lateral nunca divergir do da tabela. **O recorte que ela segue é o padrão VIGENTE da tela, e não um valor fixado aqui:** esta linha dizia "processos ativos, 169 de 649 medidos em `H-17`", e `D-33` inverteu o padrão em 04/09/2026 — a Operacional abre com os 650, não com os 170. Fixar o recorte na decisão foi o erro; quem o define é `useProcessQuery`, e a contagem espelha; **(3) a busca alcança REF, BL e contêiner (`A-39`) mais os sete destinos**, e cliente e navio ficam fora, o que **fecha o primeiro dos dois itens que `D-22` declarou em aberto** — estendê-los exigiria decidir entre a grafia da célula e o cliente consolidado, distintos desde `H-49`, e isso é regra nova em `src/domain/`. **O item (3) foi REVERTIDO por `D-34` em 04/09/2026**, e o registro fica para que esta linha não seja citada depois como recusa vigente: a busca passa a seis campos. **O motivo escrito aqui não foi ignorado — foi respondido:** `D-34` faz exatamente a escolha que esta linha adiava, e entra `clientRaw`, a grafia da célula, ficando fora `clientLabel`, o consolidado. O fechamento do item de `D-22` permanece; o que mudou foi o alcance. **A estimativa inicial de contrato novo foi refutada por medição:** `GET /api/processes` já devolve `total` respeitando filtros e `activeOnly`, e `GET /api/alerts` já respeita os filtros por `RF-18` — nenhuma das quatro toca contrato, e o que falta é a casca buscar o que o servidor já serve. **A recusa da contagem registrada no cabeçalho de `AppSidebar.tsx` não é contradita:** ela dizia que o único número disponível ignorava o recorte, e a história da contagem — `H-87` na numeração final; esta linha dizia `H-86`, que é a dos ícones — remove essa premissa em vez de ignorá-la. Custo: 4 histórias, uma P e três M, nenhuma G. **A ordem de execução foi refeita quando `D-30` acrescentou a quinta**, e a numeração passou a acompanhá-la |
| D-30 | 2026-09-03 | **A barra de treze chips dá lugar a um painel de filtros sobreposto, e a `H-60` é substituída** — barra de resumo em uma linha, painel modal à direita com os catorze em coluna | Pedido do usuário, que declarou a forma atual "muito padrão" a partir da tela real. Quatro formas foram desenhadas com os tokens do projeto e comparadas nos dois estados — recorte vazio e filtro ativo: **F1** (só os ativos na barra, o resto num botão), **F2** (os treze agrupados por natureza, com divisória), **F3** (um campo de consulta com fichas, que se fundiria com a busca por atalho) e **F4** (painel lateral com barra de resumo). **Escolhida F4**, e depois aplicada à tela inteira, o que revelou uma decisão que o mockup isolado escondia: onde o painel abre. **`3a` — sobreposto à direita, com véu — foi a escolha, contra a recomendação de `3b`**, que punha o painel no lugar da coluna de chegadas; a recomendação era não cobrir a tabela enquanto se filtra, e a escolha do usuário aceita esse custo com ele à vista. Consequências registradas: o painel **cobre** "Chegadas nos próximos 15 dias" enquanto aberto, e a coluna volta ao fechar; a barra de topo **fica visível**, porque cobrir `Aplicar alterações` esconderia a ação que grava no arquivo; e sobreposto com véu significa **diálogo modal**, com prisão de foco, `Esc` e devolução do foco — mais comportamento do que `3b` exigiria. **Ganho colateral medido no plano:** o conjunto tem um único modal, o `ConflictDialog`, cuja gestão de foco está parada em `PD-07` porque nenhuma das nove fixtures produz o conflito; o painel de filtros abre a qualquer momento e passa a ser o primeiro consumidor **exercitável** desse padrão. `FilterChip.tsx` é removido — com o painel, nenhum filtro é chip —, e `MultiSelect.tsx` sobrevive dentro dele. `H-60` permanece no backlog como registro do que existiu e linha de base da medição. Custo: M, 5 arquivos, nenhum contrato. **Ela abre o épico como `H-82`**, e não o fecha: o padrão de foco modal precisa nascer no consumidor mais exigente — véu, `inert` e 320 px —, e a busca por atalho passa a consumi-lo em vez de criar um segundo |
| D-31 | 2026-09-03 | **O cabeçalho da tabela passa a acompanhar a rolagem, e o operador escolhe quantas linhas vê** — a rolagem vertical migra para o quadro da tabela, e o tamanho de página vira 50/100/200/500 | Pedido do usuário sobre a Página Operacional. Duas arquiteturas foram desenhadas e **roladas de verdade** antes da escolha: `C1`, com a página rolando e o `<thead>` grudando no topo da janela — uma classe, nenhum efeito colateral —, e `C2`, com o quadro ganhando altura própria e a rolagem acontecendo dentro dele. **Escolhida `C2`**, e a razão é a tela ser de edição: sete colunas se editam ali desde `H-80`, e em `C1` a ação que grava no arquivo sai de vista enquanto se trabalha. Consequências: a altura do quadro precisa ser **relativa e com piso**, porque `R05` do corpus trata altura fixa em contêiner de texto como achado (`SC 1.4.12`); e o cabeçalho fixo exige `border-separate` com `border-spacing: 0`, já que o Chrome descarta a borda de um `th` fixo sob `border-collapse: collapse`. **O teto do seletor é 500, e não "todas"** — foi a troca pedida pelo usuário sobre a proposta original, e ela melhora o desenho em três pontos: com 649 processos a paginação nunca desaparece, então o rodapé tem uma forma só; o pior caso de renderização fica previsível em **4.500 células** contra 5.841; e o seletor deixa de depender do dia em que a planilha passar de 1000 linhas, que era a pergunta sem resposta de "todas". **Nenhum contrato muda:** `GET /api/processes` já aceita `limit` de 1 a `MAX_LIMIT`, que é 1000 — quem fixava 200 sem oferecer escolha era o cliente, em `PAGE_SIZE`. `25` foi descartado por não encher uma tela de 1080 px com a linha de 40 px de `H-61`. Custo: `H-84`, M, 5 arquivos. *(**Os dois números desta linha contam 649 processos, e a planilha tem 650** desde a linha criada em 02/09/2026: o pior caso comparado é **5.850**, não 5.841. Nenhuma conclusão muda — 4.500 continua menor, e a paginação continua nunca desaparecendo. `D-33`, três linhas abaixo, já usa 650.)* |
| D-32 | 2026-09-03 | **O mapa de clientes deixa de depender de cópia entre máquinas: a aplicação passa a mostrar o que falta declarar e a ser alimentada pela tela** | Pergunta do usuário — dá para gerar o `client-map.json` automaticamente ao carregar a planilha? **A geração do AGRUPAMENTO está fora, e por medição:** o cabeçalho de `client-mapper.ts` registra que a regra não é derivável do dado — dois prefixos distintos podem ser o mesmo cliente, e um prefixo de **62 processos** cobre **três**, distinguíveis só pelo importador. Heurística ali adivinharia (regra inviolável 3). **O que sobra é tudo o resto, e uma parte já estava pronta:** `saveClientRule` **já cria o arquivo** quando ele falta, desde `E13` — a proposta de fazê-lo nascer vazio ao carregar a planilha foi levantada e **descartada por já existir**. A lacuna real é outra: `mapped` existe no domínio e **nenhuma rota o expõe**, então o operador não tem como saber o que ainda não declarou. `H-88` fecha isso com rota própria — `GET /api/clients/pending` — e um painel na **Página Configuração**, ambas escolhas do usuário sobre as alternativas desenhadas: a Página Clientes foi recusada por ser analítica, e pendurar o dado em `GET /api/filters/options` ou em `GET /api/processes` foi recusado por misturar perguntas — e, no segundo caso, por obrigar o cliente a agregar, contra a regra 6. **A contagem ignora os filtros globais** de propósito: dívida de configuração não é recorte, e seguir o filtro faria a dívida sumir ao filtrar. **Consequência para `PD-08`:** com o arquivo nascendo na aplicação e sendo alimentado pela tela, deixa de haver o que copiar entre máquinas, e a janela medida em 03/09/2026 — copiar antes de atualizar a distribuição — deixa de existir. Custo: `H-88`, M, 8 arquivos, 1 contrato novo. *(**A determinação 1 foi REVERTIDA por `D-36` em 09/09/2026:** o painel vive na Página Clientes, e não na Configuração. A determinação 2 segue de pé — a contagem ignora os filtros globais —, e `D-35` declara que a 4 não é reaberta. O custo também envelheceu: `D-35` levou `H-88` de M para **G**.)* |
| D-33 | 2026-09-04 | **A Página Operacional passa a abrir com TODOS os processos, na ordem da planilha** — o checkbox inverte de "Incluir desembaraçados" para "Ocultar desembaraçados", desmarcado, e a ordenação padrão deixa de ser `eta2` | Pedido do usuário em 04/09/2026, ao usar a tela. **Contraria `RF-10` e a especificação**, que descrevem a Operacional como "tabela de processos ativos" — e por isso `RF-10` é reescrito no mesmo commit, em vez de a divergência ficar implícita (regra inviolável 1). **A definição de `A-16` NÃO muda:** "ativo" continua sendo `categoria ≠ desembaracado`; o que muda é o recorte padrão da tela, não o conceito. **Três consequências medidas em 04/09/2026:** a primeira carga passa de **170 para 650** processos; a paginação passa a existir sempre — **4 páginas** de 200, contra nenhuma hoje, porque 170 < `PAGE_SIZE`; e **271 das 650** linhas mudam de posição, embora **nenhuma** das 170 ativas mude, já que a planilha está em ordem de ETA2 e os 65 sem ETA2 ocupam as linhas 587–746. **O custo se divide em duas fatias**, e o critério é o contrato: a inversão do checkbox entra em `H-84`, que já toca os mesmos três arquivos e não muda contrato nenhum; a ordem vai para `H-89`, porque ela altera `sort` em `05-contratos-api.md`. *(**"Em ordem de ETA2" é aproximação, e a ordem exata é outra — medido em 10/09/2026** com `tools/carregar-planilha.mjs`: por `registrationDate` a planilha tem **zero** pares fora de ordem entre as 483 linhas com data, e as 167 sem data ocupam o fim contíguo a partir da linha 485 — ordenar por Registro reproduz a ordem do arquivo **linha a linha**. Por `eta2` são **27** quebras em 585. A segunda metade da frase confere: os 65 sem ETA2 estão no fim, a partir da linha 587. **Nada em `H-89` muda por isso** — a história ordena por `sourceRow`, que é a ordem física e não depende de qual critério a produziu; o registro existe porque esta linha é citada como fato sobre a planilha.)* |
| D-34 | 2026-09-04 | **A busca da Página Operacional passa a cobrir os SEIS campos de texto da planilha**, e não os três de consulta — entram Processo do cliente, Importador e Navio; o `Ctrl+K` acompanha, porque serve a mesma rota | Pedido do usuário em 04/09/2026, ao usar a tela: "a busca deve ser pra qualquer coluna". **É reversão do item (3) de `D-29`, escrito um dia antes**, e a segunda reversão de determinação registrada no projeto — a primeira é `D-21`. `D-29` fechou o alcance em três campos dizendo que "cliente e navio ficam fora", e o motivo dele era concreto: estender obrigaria a **escolher** entre a grafia da célula e o cliente consolidado, distintos desde `H-49`. **Esta decisão faz a escolha em vez de contorná-la** — entra `clientRaw`, a célula; fica fora `clientLabel`, o consolidado —, então o que caiu foi o alcance, não o raciocínio. **Contraria também `A-39`**, que resolveu a lacuna de §2 justamente delimitando REF, BL e CNTR — o achado ganha a divergência registrada, e não correção silenciosa (regra inviolável 1). **"Qualquer coluna" foi recortado, e o critério é a procedência do dado:** os seis são texto de célula, buscáveis sem inventar formatação. Ficaram de fora, por escolha do usuário sobre as alternativas desenhadas, **Cliente** — que é o nome consolidado de `client-map.json` e não uma célula, então um acerto ali não seria explicável pela planilha —, **ETA2** — data, que exigiria comparar o texto formatado `30/12/2025` — e **Categoria**, rótulo derivado de cinco regras cujo recorte o filtro global já oferece. **O `Ctrl+K` de `H-83` acompanha**: as duas buscas usam `GET /api/processes`, e separá-las exigiria parâmetro novo na rota para sustentar dois comportamentos com o mesmo nome. Custo: `H-90`, P — um `as const` no domínio, nove rótulos e os testes |
| D-35 | 2026-09-08 | **O mapa de clientes passa a ser operável pela tela em três frentes — declarar por PREFIXO, agrupar clientes, e editar o mapa de equipe —, e as três entram por uma branch de integração** | Pedido do usuário em 08/09/2026, ao ver o que `H-88` entregaria. **A medição do dia derrubou o desenho original:** as **111 grafias** sem cliente declarado caem em **16 prefixos aparentes**, e **três deles cobrem 98 grafias e 124 dos 140 processos** — com regra `exact`, a única que a tela sabe criar, o painel de `H-88` pediria **111** declarações manuais, 83 delas valendo um processo cada. Por isso o prefixo **entra em `H-88`** em vez de virar história posterior: sem ele o painel é uma lista de tarefas, não uma ferramenta. **A pré-visualização do alcance é obrigatória, e o motivo está nos dados do operador:** `Y` e `YT` são prefixos distintos na planilha, e `Y` **casa** `YT-769` — declarar os dois na ordem errada faria 62 processos mudarem de dono em silêncio, que é o modo de falha que o cabeçalho de `client-mapper.ts` já registrava com o prefixo de 62 processos cobrindo três clientes. **A determinação 4 de `D-32` NÃO é reaberta:** a aplicação continua sem sugerir agrupamento — quem digita o prefixo é o operador, e a tela só mostra o que a regra dele capturaria antes de gravar. **`groups[]` sai de `fora de escopo` e entra em `H-88`**, e a edição do `team-map.json` vira `H-91`: nenhuma das duas tem escrita hoje, e a segunda é caminho de escrita novo, com o ponto de injeção que a regra inviolável 7 exige. *(Esta linha dava a `groups[]` uma história própria, `H-91`, e o usuário a **absorveu em 08/09/2026** ao descrever o comportamento que quer: para ele não há dois conceitos — há um nome que recebe conjuntos da coluna CLT, e o grupo é o que acontece quando o **segundo** conjunto chega ao mesmo nome. Separá-los obrigaria a tela a expor a distinção que a simplificação existe para apagar. O gatilho da conversão é medido no mapa dele: `AV → Vivi` cria o cliente, `YT → Vivi` transforma-o em pai com os dois por filhos, cada um nomeado pelo VALOR da regra — `AV` e `YT`, e não pelo rótulo do pai, que produziria "Vivi > Vivi". A `H-92` do team-map assumiu o número `H-91`, o que é seguro porque nenhuma das duas foi commitada.)* **As três entram na branch `E14/clientes-editaveis`** e chegam à `main` num PR só, por escolha do usuário: a tela só faz sentido completa — declarar sem agrupar, ou agrupar sem apagar, é meio caminho que o operador não usa. O custo aceito é o conflito de uma branch longa sobre os mesmos arquivos; a mitigação é a ordem sequencial e o PR assim que as três fecharem. Custo: `H-88` cresce para **G**, mais `H-91` e `H-92`, ambas M. *(**O PR único não aconteceu, e o desfecho é do dia seguinte.** `H-88` chegou à `main` **sozinha**, pelo **PR #123**, em 09/09/2026, e `H-91` vai num segundo PR. O corpo do #123 ainda afirma, literalmente, "**A branch continua aberta.** `H-91` entra nela antes do merge, como `D-35` definiu — as duas chegam à `main` num PR só." A frase ficou falsa **no ato do merge**; ela não se corrige, porque corpo de PR mesclado é registro, não documento — e é por isso que a emenda mora aqui. **O motivo é que `H-88` ficou usável sozinha:** ela declara, agrupa e apaga, que é o que a tela cobre do `client-map.json` — o qualificador `importer` fica fora dela por decisão de `05-contratos-api.md` —, e o argumento do PR único — "declarar sem agrupar, ou agrupar sem apagar, é meio caminho que o operador não usa" — **não alcança `H-91`**, que é outro arquivo e outra página — o painel de clientes vive na Página Clientes desde `D-36`, e o de equipe nasce na Configuração. Segurá-la seria adiar entrega pronta por uma história de outro mapa. **O custo que esta linha aceitou deixou de ser pago** — o conflito de uma branch longa sobre os mesmos arquivos —, e a mitigação declarada, "o PR assim que as três fecharem", dá lugar à regra ordinária do projeto: **cada história chega quando fecha**. `H-91` nasce em branch própria, sobre a `main` que já traz `H-88`.)* |
| D-36 | 2026-09-09 | **O painel do mapa de clientes muda da Página Configuração para a Página Clientes, acima dos gráficos** | Pedido do usuário em 09/09/2026, depois de **usar** a tela pronta. **É reversão da determinação 1 de `D-32`**, que o pusera na Configuração dizendo que "a Página Clientes é analítica, e dar dois papéis a ela foi recusado" — e é a terceira reversão de determinação registrada no projeto, depois de `D-21` e `D-34`. **O motivo dela não foi ignorado, foi respondido pelo uso:** declarar cliente e olhar o ranking de clientes são o mesmo trabalho, e separá-los obrigava a trocar de página para conferir o efeito de cada declaração — que é justamente o que o operador faz a cada uma das grafias pendentes — **187 sem cliente declarado, de 509 distintas** (medido em `H-88`, 09/09/2026); esta linha dizia 129, número que nenhuma medição do repositório sustenta. **A determinação 2 NÃO é afetada:** o painel continua ignorando os filtros globais, mesmo hospedado numa página que os respeita; dívida de configuração não é recorte. **O painel virou componente** — `web/src/components/ClientDeclaration.tsx` —, e não é mais parte de página nenhuma: a página que o hospeda pode mudar de novo, e ele só depende de `dataVersion`. **Ele é montado ANTES e FORA do estado dos rankings**, e isso é consequência da mudança: os rankings têm `return` antecipado nos quatro estados, e o painel escondido quando o indicador falha tiraria do operador justamente a ferramenta que conserta o que o ranking mostra. Custo: nenhum arquivo novo além do componente, e os cinco `describe` do painel migraram de `WorkbookSetup.test.tsx` para `Clients.test.tsx` |
| D-37 | 2026-09-09 | **O painel de clientes vira uma FAIXA recolhida no topo da Página Clientes, e os três rankings passam a dividir uma grade de três colunas** | Pedido do usuário em 09/09/2026: o layout vigente lhe gerava desconforto, e ele pediu opções nos padrões da indústria. **O desconforto tem causa medida, e não é impressão** — a 1920×1080 a página carregava **duas famílias de largura e dois eixos**: o painel em `x=549` com 1024 px, os rankings em `x=240` com 813, e o olho não encontrava uma coluna única para descer. Somavam-se **710 px de ferramenta antes do primeiro gráfico**, numa página de **1752 px** contra uma janela de 1080, e **840 px vazios** ao lado de Mercadorias, sozinha na segunda linha de uma grade de duas. **Cinco arranjos foram desenhados e comparados** — faixa recolhida, abas, coluna lateral, gaveta a partir do ranking, e o alinhamento simples —, e o usuário escolheu a faixa. **Ela é a única que resolve os três problemas sem contrariar `D-36`:** as abas e a gaveta separariam de novo declarar e conferir, que é o que `D-36` juntou; a coluna lateral quebra abaixo de 1280 px, e a Página Clientes é antes de tudo de leitura. **Recolhida, a faixa custa uma linha em vez de 710 px, e a dívida continua à vista**: o resumo vive FORA do contêiner recolhível, e é o único lugar onde ela aparece sem o operador pedir. **A grade dos rankings ficou em PARES**, e essa metade foi emendada no mesmo dia: a primeira versão pôs os três na mesma linha para não deixar espaço vazio, e o usuário preferiu dois lado a lado com o terceiro embaixo do primeiro, ocupando **uma** coluna — o espaço ao lado fica reservado, do mesmo tamanho, para a quarta dimensão quando ela existir. Os 840 px que a primeira versão tratava como desperdício passam a ser lugar guardado. Custo: um estado de aberto/fechado no componente. *(**O custo declarado aqui era falso, e a correção é do mesmo dia:** esta linha dizia "`items-start` na grade para o card sozinho não esticar", e a classe **não tem esse efeito** — `align-items` opera no eixo de bloco, e um item sozinho na sua linha de grade DEFINE a altura dela, então `stretch` nele é no-op. Medido na própria página, num Chrome real a 1920×1080: **com** a classe o par sai 478/382 e Mercadorias 440; **sem** ela, 478/478 e Mercadorias 440 — o card sozinho não estica em nenhum dos dois. O único efeito observável era impedir o par de ficar uniforme, que virou reclamação horas depois. A classe saiu, e a intenção de `D-37` passou a ser cumprida em vez de contrariada. Achado por um painel de seis lentes independentes, e reconferido por medição direta antes de aceito.)* |
| D-38 | 2026-09-10 | **O caminho de volta à ordem da planilha vira um BOTÃO explícito, e a frase do estado vazio sai da barra de filtros** | Pedido do usuário em 10/09/2026, ao ler o critério de aceite de `H-89`: *"o ponto nem sempre é eficiência, é entender como a cabeça do usuário funciona"*. **É reversão do segundo critério de aceite de `H-89`**, que mandava o terceiro clique no cabeçalho voltar a `sourceRow`. **O motivo do ciclo de três não foi ignorado — foi recusado por outro critério:** ele é mais rápido e é **mudo**; nada na tela anuncia que o terceiro estado existe, e quem sabe precisa lembrar em que ponto do ciclo está. **O terceiro clique SAI em vez de conviver com o botão:** dois caminhos para a mesma coisa, um deles invisível, é o oposto de explícito — o cabeçalho volta a `asc` ↔ `desc`. **A posição foi decidida por medição, e a primeira recomendação estava errada.** O botão nasceu proposto para a barra de filtros, e quatro desenhos julgados por três lentes mostraram que aquela barra é da **casca**: `App.tsx` a monta em **seis** páginas, e `sort` só existe numa. Ele vai para a faixa de controles da própria página, onde já vivem a busca e o `Ocultar desembaraçados` — irmãos de `sort` no mesmo hook —, a **17 px** do cabeçalho que produziu a ordenação contra ~1474 px do canto direito da faixa de cima em 1920, e do lado de dentro do `<main>`, enquanto a faixa de cima fica do outro lado do "pular para o conteúdo". Ele é o **último** da linha e encosta à direita por `ml-auto`, então aparecer e sumir não desloca o `Nova linha`. **A frase "Todos os processos, sem recorte" sai junto**, por escolha do usuário entre três alternativas: "recorte" é vocabulário do projeto e não do operador, e a frase **afirmava o falso** com `Ocultar desembaraçados` ligado — 170 de 650 à vista —, porque o checkbox vive em `useProcessQuery` e a barra, que é da casca, não o enxerga. Calada, ela não mente; o checkbox fica como está, por decisão do usuário. Custo: `H-89` ganha `SORT_LABELS` e `clearSort` no hook, um bloco em `Operational.tsx`, e `ProcessTable` passa a consumir o mapa de rótulos em vez de literais |
| D-39 | 2026-09-10 | **O recorte deixa de ser uma frase e vira FICHAS descartáveis, e o número do gatilho passa a contar valores** | Pedido do usuário em 10/09/2026, a partir da tela real: o botão dizia `3` sobre "Categoria: Em andamento · Cliente: AV · e mais 1", e ele quis o número dos **itens marcados** e um `x` por item. **`activeCount` NÃO muda de significado**, e isso é deliberado: a Página Performance escreve "N filtros ativos" ao descrever o escopo, e trocar o sentido dele mentiria lá — nasce `activeValueCount` ao lado. **Não é reversão de `D-30`, e a diferença é o verbo.** Lá cada chip era o **gatilho** de um filtro e abria um popover: eram treze fixos, `H-60` mediu **1437 px contra 1064 disponíveis** — duas linhas entre 1280 e 1600 px — e `H-65` mediu **6 dos 13 popovers** empurrando a página para fora a 320 px (`VN-1`, `SC 1.4.10`). Aqui a ficha **não abre nada**: o `x` descarta, e a ação já existia (`toggle` tira um valor e preserva os demais). **O teto é quatro fichas**, escolha do usuário entre três formas desenhadas — teto com contagem, rolagem lateral, e quebra de linha, que é o que `D-30` recusou. Ele não é decoração: medido na planilha real em 10/09/2026, os filtros oferecem **509** valores em Processo do cliente, **220** em Mercadoria e **133** em Cliente. **A frase saiu inteira**, e com ela `activeDescriptions` e `summarySentence` — o resumo textual e as fichas diriam a mesma coisa duas vezes. Custo: `H-92`, P, 4 arquivos, nenhum contrato; entregue junto de `H-89` por escolha do usuário, já que as duas tocam a mesma faixa |
| D-40 | 2026-09-10 | **A cor deixa de definir o responsável, e "Sem responsável" passa a ser destino** | Decisão do usuário em 10/09/2026, ao ser perguntado o que fazer com um membro sem critério nenhum: *"não leve mais em consideração essas cores para definir o responsável. Deverá ser levado em consideração o que for definido no painel."* **Isto REABRE `D-23`** e reverte a determinação 2 de `H-91` — a **nona** reversão de determinação do projeto, pela lista canônica da §5. **O argumento que sustentava a cor deixa de valer por mérito, e não por decreto:** `D-23` existia porque, sem mapa de equipe e sem tela para editá-lo, o campo Responsável nasceria vazio nas 649 linhas; com `H-91` entregue, quem não tem importador declarado passa a ter onde declará-lo. **O `fallback` cai junto**, e por escolha explícita: *"o fallback deve cair no Sem responsável, que aí o usuário já sabe que tem que definir um"* — é a regra inviolável 3 aplicada ao próprio mapa, cobertura total trocada por enxergar o que não se sabe. **Custo medido em 10/09/2026, sobre as 649 linhas reais:** hoje 559 processos têm responsável pelo importador, **48 só pela cor** e 42 por nada; depois, 559 e **90**. Os 48 que migram são **todos ativos** — 13 de `MPA` e 35 com IMPORTADOR em branco —, e hoje **nenhum** processo ativo fica sem responsável. As 35 de importador vazio não são alcançáveis por mapa nenhum, e sim preenchendo a coluna C, editável desde `H-80`. A anomalia `RESPONSAVEL_DIVERGENTE` sai **antes de ter detectado a primeira ocorrência**: zero em 31/08 e zero em 10/09. Custo: `H-93`, M, 12 arquivos |
| D-41 | 2026-09-10 | **A cor de cada CÉLULA vai para a tabela como fundo, e a casca da tabela recua de tom** | Pedido do usuário em 10/09/2026, depois de confirmar que *"existem sim linhas que seguem toda de uma cor e têm uma célula com cor diferente"*. **A medição confirmou e foi além:** 36 das 649 linhas divergem internamente dentro de A–L, e as colunas `Coluna 13` e `R$ ENVIADO` têm paleta própria — 13 cores distintas nas células contra as 9 que `config/color-map.json` conhece, com **1.237 células** pintadas por chaves que o mapa ignora. Isso **refuta `A-44`**, que afirma que "as colunas K e L acompanham a cor da linha" e que "N é ciano fixo". **`ADR-0003` NÃO é tocado:** a cor do processo continua saindo da âncora, e as demais colunas seguem ignoradas *para esse fim* — renderizar não classifica. **A exceção de contraste é declarada, e é escolha do usuário:** os dois roxos reprovam a WCAG AA com qualquer cor de texto (3,49 e 3,41 com `#14161a`; 4,05 e 3,96 com preto puro), e ele escolheu o fundo inteiro com a frase *"quero ver como fica primeiro"* — a cor é dado do arquivo dele, não escolha do autor. **O rebaixamento de tom cabe em exatamente um degrau**, nos dois temas: `#ffffff` → `#f4f5f7` no claro e `#131519` → `#1b1e24` no escuro; o degrau seguinte leva o texto `muted` a 4,51 no claro (`#6a6f77` sobre `#f0f2f5`) e a 4,18 no escuro (`#81878f` sobre `#23262c`), contra o piso de 4,5 de `H-39` — o `secondary` sobrevive aos dois, em 6,20 e 6,49. **Consequência obrigatória:** o realce de linha deixa de ser fundo e vira contorno — `#f0f2f5` mede 1,03 contra o novo fundo e some sob célula pintada. **A cor fica como aparência**, por decisão do usuário em 11/09/2026 — perguntado o que significam o verde-claro e o ciano em `R$ ENVIADO` e o amarelo em `STATUS`, respondeu *"deixe assim por enquanto"*. Custo: `H-94`, M, 18 arquivos |
| D-42 | 2026-09-11 | **Tons próximos se unificam por lista DECLARADA de cor de exibição, e as três chaves brancas ficam unificadas** | Escolha do usuário em 10 e 11/09/2026, sobre a alternativa de unificar por limiar calculado. **Confirma a recusa da alternativa A2 do `ADR-0003`** e acrescenta o campo que faltava: `config/color-map.json` **não tem hex nenhum hoje** — conferido nas 9 entradas —, então antes desta decisão o pedido de `D-41` não era implementável. **Duas chaves com o mesmo `display` ficam unificadas por construção**, sem estrutura nova. **Os números autorizam a lista curta:** dos 78 pares de cores, **5 estão abaixo do limiar de percepção** (0,00 · 0,21 · 0,62 · 1,77 · 1,77) e o sexto salta para **15,88** — abismo de fator 9, o mesmo padrão que a medição de 04/09/2026 já achara entre as cores do mapa. **As três brancas foram unificadas a pedido**, medidas em 11/09: `argb:FFFFFFFF` (466 células, só em M e N), `theme:0|tint:0.0000` (56 células, e a **linha 485 inteira de A a L** — a "1 linha branca" de `H-01`) e `argb:FFF6F8F9` (32 células). **A `styleKey` continua literal:** o `display` é declaração de aparência, e nunca resolução de `theme` mais `tint`, que `src/io/style-extractor.ts` se recusa a fazer |
| D-43 | 2026-09-11 | **A coluna derivada Cliente sai da tabela, entram as 16 da planilha com o nome literal do cabeçalho, e a coluna descoberta é DESCARTADA** | Pedido do usuário em 10/09/2026 — *"todas as colunas devem aparecer na tabela do operacional, com seu exato nome da planilha original a tal ponto que se for criada uma nova coluna, ela deva aparecer"* —, **com a segunda metade descartada por ele em 11/09**, depois de ver o custo. **O motivo do descarte é a regra inviolável 3:** uma coluna nova não diz se é texto ou data, se conta para `onlyRefFilled`, se é editável nem se é filtrável, e responder por ela seria adivinhar; as três colunas de tipo misto medidas (N, O e J) provam que a generalização erra, e `EDITABLE_FIELDS` **já diverge do arquivo hoje em 46 células** — 44 na coluna `N`, declarada `text` e preenchida com data formatada; 1 na `J`, declarada `text` e com número; e 1 na `O`, declarada `date` e com número sem formato de data —, achado a documentar e não a corrigir em silêncio. **As 16 colunas já viajam no DTO**, então exibi-las é mudança de cliente. **`RF-32` e `RF-33` ficam emendados** — `RF-32` diz "nove colunas" e `RF-33` diz "sete colunas editáveis"; o título de `H-77` também diz "nove colunas", e fica como está. **Nenhuma funcionalidade se perde**, condição declarada pelo usuário: a consolidação de cliente fica inteira, e ele explicou para quê — *"a única intenção de fazer agrupamentos como de Vivi é que apareça subdividido nos gráficos"*. **A única capacidade removida** é declarar o cliente a partir de um processo (`RF-35`), cujo único consumidor é a coluna que sai; a ressalva foi apresentada em 11/09 e ele manteve a decisão. **A escolha de colunas visíveis nasce junto**, porque as 16 medem cerca de 3.000 px contra 1.023 px das 7 de planilha de hoje. **`H-96` fica escrita e não executada**, por escolha dele: o vínculo por letra corrompe **616 de 650** processos e **580 categorias** ao deslocar uma coluna, sem quarentena e sem anomalia — mina, não incêndio. Custo: `H-95` M com 16 arquivos, `H-96` G com 12 |

**D-13 e D-15 nasceram do protocolo de fatia** (`CLAUDE.md`): as duas lacunas
apareceram ao montar o checklist de `H-02` e `H-03`, antes de qualquer código.

**Padrão observável:** D-05, D-06 e D-07 são o mesmo tema — persistência e
escrita — decidido em três rodadas. O protocolo da §2 existe para que a próxima
oscilação custe uma conversa, e não um plano refeito.

**D-21 é a primeira reversão de uma determinação escrita pelo próprio plano**, e
não de uma resposta anterior do usuário: quem havia decidido que o modo escuro
ficava de fora era o cabeçalho de `E9`, com o argumento de que o benefício era
modesto — um operador, uma máquina. O argumento continua correto e deixou de ser
decisivo. O registro existe para que a determinação de `E9` não seja citada
depois como recusa vigente.

**Reversão tem critério, e ordinal dentro de célula envelhece.** Reversão aqui é
*determinação já escrita — neste log, num ADR, num RF ou no cabeçalho de um
épico — que deixa de valer*; mudar de ideia sobre uma resposta anterior não
conta, porque é o que o log registra por natureza. Pelo critério, em ordem de
data: `D-05` (a virada de escopo do briefing), `D-07` (parcialmente `D-06`),
`D-21` (o cabeçalho de `E9`), `D-25` (a negativa do `ADR-0004`), `D-33` (`RF-10`,
reescrito no mesmo commit), `D-34` (o item 3 de `D-29`), `D-36` (a determinação
1 de `D-32`), `D-38` (o segundo critério de aceite de `H-89`), `D-40` (a
determinação 2 de `H-91`, com a reabertura de `D-23`) e `D-43` (a revogação de
`RF-35`), mais a emenda que `D-35` aplica a si mesma. **Os ordinais escritos
em `D-34` ("a segunda") e `D-36` ("a terceira") não contam `D-25` nem `D-33`** —
ficam como registro do que se sabia no dia, e esta lista é a fonte;
`docs/06-backlog.md` copiou o ordinal de `D-36` e carrega a mesma ressalva.

---

## 6. Commits

Conventional Commits, em pt-br, com escopo por área.

```
<tipo>(<escopo>): <descrição em pt-br>

- detalhe técnico do que mudou
- o "porquê", se for crítico
```

Tipos: `feat` · `fix` · `chore` · `refactor` · `docs` · `style` · `perf`
Escopos: `(domain)` · `(io)` · `(app)` · `(http)` · `(web)` · `(tools)` ·
`(config)` · `(docs)` · `(claude)` · `(repo)` — a mesma lista do `CLAUDE.md` e da
skill `/sugerir-commits`. `(tests)` nunca foi usado e saiu: a *definition of done*
exige teste dentro da própria fatia, então commit só de teste é raro por
construção — e o tipo `test` não existe aqui pelo mesmo motivo.

Regras:

- **Um commit por ponto verde**, e não por história — a regra vigente está no
  `CLAUDE.md`: o corte é o momento em que uma preocupação fecha **e** o portão
  passa, porque é isso que mantém o `git bisect` utilizável. Dentro de uma
  história os pontos verdes caem sozinhos na cadeia canônica: contrato fechado,
  domínio verde, rota verde, interface verde.
- **Nunca commitar automaticamente.** Sugerir mensagem só quando pedido, e
  trazer o `git add` correspondente junto.
- **Nunca commitar** `*.xlsx` fora de `tests/fixtures/`, `config/app.json` ou
  `data/` — o `.gitignore` protege, mas confira.

---

## 7. O que deliberadamente **não** está aqui

Registrado para que a ausência seja escolha, não esquecimento. **Duas delas
acabaram** — branch e release —, e ficam na tabela com o que passou a valer:
apagá-las esconderia que a escolha mudou.

| Ausente | Por quê |
|---|---|
| Política de branch | **Deixou de ser ausência.** O argumento de 03/08/2026 — "um desenvolvedor, branch por história seria cerimônia sem revisor" — caiu quando o portão passou a fazer o papel do revisor: branch por história ou `<tipo>/<escopo>-<descrição>`, merge só por PR, `main` protegida com `bypass_actors` vazio. Vive em `CLAUDE.md` |
| Processo de code review | Não há segundo revisor **humano**. Os substitutos, em camadas: `revisor-xml` e `revisor-estilo` em `.claude/agents/`, a guarda de contrato em `tests/repo/`, o portão local `npm run verify`, e `verify.yml` + `dados-sensiveis.yml` como checks obrigatórios do PR |
| SLA de resposta, RACI, matriz de aprovação | Duas pessoas |
| Versionamento semântico do produto | Aplicação local sem distribuição pública |
| Processo de release | **Deixou de ser ausência, e não é `git pull`.** O release é a branch `distribuicao`, calculada por `scripts/sincronizar-distribuicao.ts` a partir da `main` mesclada e **baixada como árvore** na máquina do operador — que não é repositório git (medido em 04/09/2026, ao fechar `PD-01`). O detalhe está em `.claude/rules/distribuicao.md` |
| Política de dependências | A stack está fixada em `CLAUDE.md`. **Acrescentar dependência é decisão do log da §5, não de ADR** — foi assim em `D-13` (Biome) e `D-17` (Testing Library e jsdom), e nenhum ADR nasceu desde o 0006 |

Se alguma dessas passar a fazer falta, o gatilho é o mesmo dos ADRs: quando a
ausência causar um problema concreto, e não antes.
