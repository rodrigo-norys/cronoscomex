# Regra de fronteira

Nada aqui importa de `src/io/`, `src/app/`, `src/http/` ou `web/`, nem de
modulos de I/O do Node. Verificado por `biome.json` (override em
`src/domain/**`) e quebra a build. Ver ADR-0006.
